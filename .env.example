# Required. Supply real project values locally; never commit .env.local.
APP_ENV=
APP_BASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
LOG_LEVEL=
# Test-only disposable PostgreSQL URL. Never point tests at a hosted database.
TEST_DATABASE_URL=
# Migration credential belongs only in approved CI/deployment secret storage.
DATABASE_MIGRATION_URL=
# No service/secret key is used by the Phase 1 application.
# Phase 6B: Resend Email Provider (Variable names only; never commit real credentials)
RESEND_API_KEY=
RESEND_WEBHOOK_SECRET=
